# vim: ft=zsh
# doctor: brewup requires: brew
(( $+commands[brew] )) || { unfunction brewup; return 0 }

brew update \
  && brew upgrade \
  && brew cleanup \
  && brew bundle dump --force --file ~/.Brewfile
